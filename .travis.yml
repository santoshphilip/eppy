language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
# platforms
os:
  - linux
  - osx
  - windows
# command to install dependencies
install:
  - pip install .
  - pip install -r requirements.txt
# whitelist
branches:
  only:
    - travis_trial
# command to run tests
script: py.test

